# 浏览器登录开发服务器

在浏览器中打开 RepoReaper 开发服务器界面。

## 描述

此命令会在浏览器中打开开发服务器地址（http://127.0.0.1:8000），方便快速访问 GitHub RAG Agent 界面。

## 使用场景

- 开发服务器已启动（通过 `make run-dev` 或 `make run`）
- 需要快速访问 Web 界面进行测试
- 查看服务状态和功能

## 注意事项

- 确保开发服务器已运行（端口 8000）
- 如果服务未启动，请先运行 `make run-dev` 或 `make run`